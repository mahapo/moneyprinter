{
  "private": true,
  "author": "Manuel Haller Polo",
  "workspaces": [
    "bot",
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "deploy": "pm2 deploy production",
    "dev": "concurrently --kill-others \"cd frontend && yarn && PORT=4000 yarn dev\" \"cd backend && yarn dev\"",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "release:patch": "yarn config set version-tag-prefix '' && yarn version --patch && git push --tags",
    "release:minor": "yarn config set version-tag-prefix '' && yarn version --minor && git push --tags",
    "release:major": "yarn config set version-tag-prefix '' && yarn version --major && git push --tags"
  },
  "devDependencies": {
    "@babel/core": "^7.12.13",
    "@babel/node": "^7.12.13",
    "@babel/preset-env": "^7.12.13",
    "@types/jest": "^26.0.20",
    "concurrently": "^5.3.0",
    "husky": "^4.3.8",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "lint-staged": "^10.5.4",
    "pm2": "^4.5.4",
    "prettier": "^2.2.1",
    "ts-jest": "^26.5.0",
    "typescript": "^4.1.3"
  }
}
