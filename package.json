{
  "private": true,
  "workspaces": [
    "backend",
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "deploy": "pm2 deploy production"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "pm2": "^4.4.0"
  }
}
