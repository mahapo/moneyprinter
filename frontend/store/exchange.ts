export const state = () => ({
  loading: false,
  dark: false,
  drawer: null,
  color: 'success',
})

export const mutations = {}

export const actions = {
  async startWebSocket({ dispatch }) {},
}
